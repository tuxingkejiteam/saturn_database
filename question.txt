


* 本地的 json 和 数据库中的内容是不是同步的，如果不是同步的什么时候进行同步

* 如果是同步的，本地的 json 存在的价值是什么，为什么不直接入库

* 是不是需要将图像都规范到 jpg 固定格式，还是允许存在 png JPG 的样式存在

* json 的格式设计不合理，为什么还要加上 序号作为 key ？

* 先做一个版本，不需要数据库，需要维护的表是 record.json all_md5.json （同一个 md5 可以申请不一样的 uc ）

* 数据库的主要作用是什么？

    * 我认为数据库的主要作用是方便数据存储，持久化，查找，

    * 数据库中不存标注的具体信息是出自于什么样的考虑

    * 或者数据库除了一张 md5_uc 表和 一张 record 表之外，其他的表不是实时更新的

    *

# ----------------------------------------

* id 在哪里进行更新， labelimg 中是不带 id 信息的，修改之后的 xml 转为 json 后也是不携带 id 信息的，如何确保两次编辑后的同一个目标的 id 是一致的



* 更新json数据 update_json_list_to_db()
入参：图片对应的json文件路径列表，必须为已重命名的格式
出参：是否更新成功，返回的是 bools list
功能：对于未录入json文件，信息插入数据库当中，对于已录入的 json 文件，更新其中不一样的参数（需要讨论一下，之前的属性是不是得全部删除，update == del + add ？）


* 实际使用的时候存在问题，使用标注软件修改 xml 之后。Xml中就会丢失对应的信息（软件的限制），所以对于有些属性，只能通过 opt 类进行修改，而不能在 update 的时候因为 json 中没有对应的属性就去数据库中删除对应的部分，

后续需要完善 opt 中 update attr from db， 定期维护一次

*         # fixme 我提供对应的接口，是不是存 uc txt 我不去管，但是需要通过接口才能去改变对应的属性

# ----------------------------------------

* 数据库查询的速度是不是限制于 表的行数，还是表的大小（w * H ）

* 不用大表，直接用一个很小的表，表示一个 uc 对应多种标签

*




















